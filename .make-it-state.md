# Project State -- API Gateway
> Last updated: 2026-04-07
> Last session: resume-it (battle testing + data contracts + developer portal + caching + observability)
> Session ended: 2026-04-07

## Current Status
Kong CE 3.9 API Gateway with admin panel (FastAPI), Next.js 14 frontend, AI layer (Claude),
PostgreSQL, Redis. All services containerized with Docker Compose. Admin panel fully functional
with OIDC auth, RBAC, team management, API registry with data contracts, subscriber management,
public API catalog with developer portal (Swagger UI Try It), and per-API caching policies.

## This Session
- Rebuilt admin panel and re-ran full battle test suite
- Fixed 5 bugs discovered during testing
- Expanded test suite from 101 to 174+ tests (12 files)
- Implemented data contracts feature (18 columns, contract endpoint, public catalog)
- Implemented developer portal with Swagger UI "Try It":
  - GET /public/api-catalog/{slug}/try-it returns embedded Swagger UI
  - Loads API's OpenAPI spec, pre-configures gateway URL
  - Subscribers authenticate via API key in Swagger UI authorize dialog
- Implemented per-API caching policies:
  - 6 new columns: cache_enabled, cache_ttl_seconds, cache_methods, cache_content_types, cache_vary_headers, cache_bypass_on_auth
  - Kong proxy-cache plugin synced on activation and contract update
  - Safe defaults: disabled, memory strategy (Kong CE limitation)
  - Migration: database/migrations/006_caching.sql
- Created observability comparison document (docs/observability-comparison.md):
  - Maps Azure Monitor features to Prometheus + Grafana + Cribl stack
  - 36 alert rules, 6 dashboards, 5 Cribl pipelines, 14 recording rules
  - Cost comparison: $0 additional licensing vs APIM per-unit pricing
- Updated all documentation (api-reference, self-service-guide, admin-guide, architecture, kong-gateway, RFC-001, README, TODO)

## Outstanding Items
- Rebuild DB and admin panel to apply migration 006
- Run full test suite to verify all ~190 tests pass
- Fix AI prompts auth gap (/ai/prompts allows unauthenticated access)
- Push commits to both remotes (sleepnumberinc/api-gateway and sealmindset/api-gateway)

## Test Status
- Integration tests: ~190 tests across 12 files (pending verification after rebuild)
- New tests: test_11 developer portal tests (4), test_12 caching tests (12)
- Last verified run: 174 passed + 1 xfail (before new features)

## Known Issues
- `/ai/prompts` endpoint allows unauthenticated access (marked xfail in tests)
- Kong CE proxy-cache only supports memory strategy (not Redis)
- `typescript` file in repo root is a terminal recording, not code (pre-existing)

## Suggested Next Steps
1. Rebuild and run full test suite
2. Fix AI prompts auth gap (add require_permission to AI router)
3. Push to both remotes
4. Send RFC to APIM teams for review
